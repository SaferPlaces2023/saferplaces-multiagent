# PLN-002 — Aggiornamento suite `tests/` al codebase corrente

## Contesto

La cartella `tests/` era stata creata alcune settimane prima in parallelo allo sviluppo del
codebase. Nel frattempo l'architettura era evoluta aggiungendo tre nuovi subgraph (retriever,
models, layers) che non venivano tracciati né testati dalla suite esistente:

- `AGENT_REGISTRY` in `_utils.py` mappava solo 5 agenti (request_parser, supervisor_agent,
  supervisor_planner_confirm, supervisor_router, final_responder); i nuovi nodi
  (`retriever_agent`, `retriever_invocation_confirm`, `retriever_executor`, `models_agent`,
  `models_invocation_confirm`, `models_executor`, `layers_agent`) non erano presenti → il
  loro output `[agent_name]` veniva silenziosamente ignorato dalla chain visualizzata.
- `tests.json` copriva solo due suite: T001 (greeting) e T002 (supervisor + plan
  confirmation); nessuno scenario esercitava il retriever subgraph, il models subgraph o il
  flow di rigetto/replanning del piano.

---

## File modificati

| File | Tipo di modifica |
|---|---|
| `tests/_utils.py` | Aggiornato — nuovi import + espansione `AGENT_REGISTRY` |
| `tests/tests.json` | Aggiornato — aggiunte suite T003, T004, T005 |

---

## Dettaglio modifiche

### `tests/_utils.py`

**Nuovi import** (all'interno del blocco `with silence():`):

```python
from saferplaces_multiagent.ma.specialized.safercast_agent import RetrieverPrompts as _RetrieverPrompts
from saferplaces_multiagent.ma.specialized.models_agent import ModelsPrompts as _ModelsPrompts
```

**`AGENT_REGISTRY` — agenti aggiunti**:

| Chiave | Classe | System prompt |
|---|---|---|
| `retriever_agent` | `DataRetrieverAgent` | `RetrieverPrompts.TOOL_SELECTION_SYSTEM` |
| `retriever_invocation_confirm` | `DataRetrieverInvocationConfirm` | descrizione testuale (no LLM) |
| `retriever_executor` | `DataRetrieverExecutor` | descrizione testuale (no LLM) |
| `models_agent` | `ModelsAgent` | `ModelsPrompts.TOOL_SELECTION_SYSTEM` |
| `models_invocation_confirm` | `ModelsInvocationConfirm` | descrizione testuale (no LLM) |
| `models_executor` | `ModelsExecutor` | descrizione testuale (no LLM) |
| `layers_agent` | `LayersAgent` | descrizione testuale (no LLM per routing) |

Gli agenti già esistenti non sono stati modificati.

---

### `tests/tests.json`

Suite esistenti mantenute invariate (T001, T002). Aggiunte:

**T003 — Retriever subgraph**
> Esercita il path: supervisor → retriever_agent → retriever_invocation_confirm →
> retriever_executor. Copre la selezione degli strumenti DPC e Meteoblue.

Messaggi:
- `"Retrieve the latest DPC rainfall radar for northern Italy"` → `"yes"`
- `"Get the Meteoblue precipitation forecast for Rome for the next 24 hours"` → `"yes"`
- `"Download temperature forecast for Milan bbox 2025-06-01 to 2025-06-02"` → `"yes"`

**T004 — Models subgraph**
> Esercita il path: supervisor → models_agent → models_invocation_confirm →
> models_executor. Copre SaferRain flood simulation.

Messaggi:
- `"Run a SaferRain flood simulation for Rome with 50mm/h rainfall"` → `"yes"`
- `"Simulate flood propagation for a heavy-rain scenario on the Po valley"` → `"yes"`

**T005 — Plan rejection + replan**
> Esercita il flow di rigetto del piano da parte dell'utente (`replan_type: "reject"` /
> `"modify"`) e il successivo replanning del SupervisorAgent.

Messaggi:
- `"Run a flood simulation and retrieve rainfall data for Milan"` → `"no, only do the flood simulation without retrieving data"`
- `"Show me available raster layers and run a flood model"` → `"no, just list the layers"`

---

## Esecuzione

```bash
python -m tests.run T001   # greeting
python -m tests.run T002   # supervisor plan + confirmation
python -m tests.run T003   # retriever subgraph
python -m tests.run T004   # models subgraph
python -m tests.run T005   # plan rejection + replan
```

Output scritto in `tests/result/T00N.md` per review manuale.

---

## Note architetturali

- Il meccanismo di rilevamento degli agenti è **basato su parsing del print()** prodotto da
  ciascun nodo (`[agent_key] → ...`). Ogni agente non registrato in `AGENT_REGISTRY` rimane
  silente nell'output anche se invocato correttamente.
- I test rimangono **integration/E2E senza assertion automatiche**: i result file `.md` sono
  destinati alla review manuale. Non sono stati aggiunti assertion in questo piano.
- Le suite T003–T005 producono chiamate reali alle API (DPC, Meteoblue, SaferRain); richiedono
  le relative variabili d'ambiente configurate in `.env`.
